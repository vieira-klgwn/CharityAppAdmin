package vector.CharityApp.Admin.dto;

public class RoleCountDTO {
    /***
     * The RoleCountDTO is simply used to show how many users are in each role (like how many donors, volunteers, admins) in  charity app dashboard.
     *
     * 👉 In short: It’s for the dashboard stats — a quick count of users per role.
     */
}
